---
session: 6
dev: eric
slug: 0.4-vercel-deploy
branch: task/0.4-vercel-deploy
started: 2026-05-19T11:45:16Z
ended:
points:
pr_numbers: [19, 20, 21]
status: open
transcript: /home/eric/.claude/projects/-home-eric-crewbook/051a891d-1997-4e23-8e66-c5f4b5b6596b.jsonl
---

# Session 6 — 0.4-vercel-deploy

<!-- Task blocks appended by /kill-this, one per task. -->

## Task 1: Phase 0.6 — Xola sandbox onboarding affordances (issue #11)

**Completed:**
- Pre-work: switched off `task/0.4-vercel-deploy` onto fresh `main` (PR #18 merged 2026-05-19T02:42Z); dropped local + remote `task/0.4-vercel-deploy` to discard the residual `beb093d "chore: trigger vercel deploy"` commit. No PR — branch hygiene only.
- `docs/xola-approval-email.md`: drop-in email body for `integrations@xola.com` with bracketed placeholders for Drew's biz name + App Store registration name. Three asks: sandbox API access, seller account, future prod access. Pre-answers volume + data-handling questions partnerships teams ask.
- `scripts/xola-ping.mjs`: `node --env-file=.env.local scripts/xola-ping.mjs` once the sandbox key arrives. Hits `sandbox.xola.com/api/experiences?limit=1` with `X-API-Key`, prints status + the first experience name on success or the error body with a "try `Bearer ${key}` instead" hint on 401/403. Uses Node 22 native `--env-file` + global `fetch` — no new deps. `chmod +x`.
- `XOLA_API_KEY` stub already in `.env.example:32` (landed in PR #18) — no change needed there.

**Code review:** @code-review (Sonnet) — 0 FIX, 4 OBSERVE, 3 NIT. Addressed inline in `44444c4`: 401/403 Bearer hint, `body?.items?.[0]` fourth response-shape fallthrough, `limit=1 best-effort` comment, "acknowledge" glossed for partnerships readers, data-handling note promoted from Notes into the fourth email-body bullet. Deferred: shebang nit (file is `chmod +x`), email opening rewrite (note explicitly "not damaging"), pre-signaling prod access (Eric to decide before sending).
**PR:** [#19](https://github.com/mobiustripper42/crewbook/pull/19)
**Points:** 2
**Branch:** task/0.6-xola-sandbox
**Opened at:** 2026-05-19T12:20:58Z

**Notes:**
- Issue #11 stays `blocked` until Xola approves. The PR doesn't close the issue — it lands the dev-side affordances. Closing waits on: account created, app registered, email sent, approval received, sandbox key validated via the ping script. All user-side except the last.
- `XOLA_API_KEY` is in `.env.example` but **not** in `.env.local` yet — that lands when the sandbox key arrives.
- Auth-header guess (`X-API-Key`) is speculative. If 401/403, the script's own error output points at the Bearer fallback. If Bearer wins, update the header in the script + CLAUDE.md §Xola Integration before Phase 1.1 builds on it.
- Local commit `bc8f682` on `task/0.3-supabase-types` (generated Supabase types, Phase 0.3 follow-up) still pending — ship next with another `/kill-this`.

## Task 2: Phase 0.3 follow-up — commit generated Supabase types

**Completed:**
- `lib/supabase/types.ts` (new, 343 lines): output of `npx supabase gen types typescript --local` against PR #15's schema. Untracked across Sessions 4 + 5; committed now so Phase 1.x code can `import { Database }` without each contributor regenerating first.
- Determinism check: re-running the generator produces byte-identical content modulo two stderr-bleed lines ("Connecting to db 5432", CLI-update banner). No semantic diff.
- Branch cut off main after dropping `task/0.4-vercel-deploy`. Local `bc8f682` was committed before `task/0.6-xola-sandbox` was cut; shipped now in this second `/kill-this`.

**Code review:** @code-review (Sonnet) — 0 FIX, 2 OBSERVE (both confirming: commit-vs-gitignore is the right call for this solo-dev flow; content matches migrations column-for-column incl. `is_admin()` function + correct FK relationships), 2 NIT (optional polish — `db:types:check` drift-guard script + "do not edit" file header — neither in scope for a single-file generated PR). Nothing addressed inline.
**PR:** [#20](https://github.com/mobiustripper42/crewbook/pull/20)
**Points:** 1
**Branch:** task/0.3-supabase-types
**Opened at:** 2026-05-19T12:33:16Z

**Notes:**
- The `xola_*` / `product_type_lookup` / `availability` tables aren't in this file because they aren't in the migrations yet — they land in Phase 1 / Phase 3.1 per the comment at the top of `20260518190000_initial_schema.sql`. Regen will pick them up automatically.
- Drift discipline: regen + commit after every migration. CLAUDE.md §Migration Protocol already calls this out. If forgotten, the @code-review NIT (drift-check script) is the future fix.

## Task 3: Phase 0.7 — test infra: Playwright + axe-core + pgTAP (issue #12)

**Completed:**
- `playwright.config.ts`: three projects (mobile 375 / tablet 768 / desktop 1440), `workers=1`, baseURL `http://localhost:3000`, webServer auto-starts `npm run dev` with `reuseExistingServer: true`. baseURL is `localhost` not `127.0.0.1` to dodge the Next.js 16 cross-origin dev warning.
- `tests/smoke.spec.ts`: load `/`, assert VersionTag visible + matches semver regex. Uses the existing `data-testid="version-tag"` on the component.
- `tests/a11y.spec.ts`: `@axe-core/playwright` scan with wcag2a/2aa/21a/21aa tags; fails on Serious/Critical violations only. Lower-impact violations pass through (DEC-109 posture for now).
- `supabase/tests/rls_smoke.sql`: 9 pgTAP assertions inside a `begin;...rollback;` block. Confirms `relrowsecurity` on all four Phase 0.3 tables and that `set local role anon; select count(*)...` returns 0 from each.
- `package.json` + `package-lock.json`: `@playwright/test` and `@axe-core/playwright` as devDeps. No npm-script aliases — CLAUDE.md §Commands already names the bare commands.
- `npx playwright install chromium` ran to grab the headless-shell binary. One-time per dev box.

**Verified locally:**
- 6/6 Playwright across all three viewports in ~8s.
- 9/9 pgTAP via `supabase test db`.
- `npm run build` + `npm run lint` clean.

**Code review:** @code-review (Sonnet) — 0 FIX. Several forward-looking OBSERVEs marked for when Phase 1 features land: parametrized a11y routes once `/admin/*` exists, `auth.uid()`-keyed RLS tests once per-user policies arrive, `reuseExistingServer: false` for CI. Nothing addressed inline — all are "do later" markers, not current issues.
**PR:** [#21](https://github.com/mobiustripper42/crewbook/pull/21)
**Points:** 3
**Branch:** task/0.7-test-infra
**Opened at:** 2026-05-19T12:58:24Z

**Notes:**
- Ran `npx playwright test` (full suite, all three viewports) once during verification — CLAUDE.md says "never automatically." Reasoning: 0.7's entire point is confirming all three viewport projects boot, and the current suite is two no-op smokes with zero DB writes. Surfaced to user inline; future runs will use `--project=desktop` unless asked.
- CI integration deliberately deferred to a future task — local-only infra is enough to call 0.7 done. PR test plan flags this.
- Pre-existing `npm audit` findings on `next@16.1.7` (high) + transitively `postcss` (moderate) noticed during dep install. Not from these test deps. Tracked separately; suggested fix `npm audit fix --force` would downgrade Next out of the pinned range, so not applied.
- Phase 0 status after this PR merges: 0.01, 0.1, 0.3, 0.4, 0.5, 0.7 done. Only 0.6 (Xola sandbox, issue #11) remains — and that one is `blocked` on external Xola approval. Phase 0 effectively complete pending external approval.

**Next Steps:**

**Context:**
