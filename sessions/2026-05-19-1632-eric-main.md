---
session: 7
dev: eric
slug: main
branch: main
started: 2026-05-19T16:32:58Z
ended:
points:
pr_numbers: [30, 31]
status: open
transcript: /home/eric/.claude/projects/-home-eric-crewbook/3279463a-b72c-46a5-b1c7-97c9490c40be.jsonl
---

# Session 7 — main

<!-- Task blocks appended by /kill-this, one per task. -->

## Task 1: Phase 1.1 — Xola API client foundation (issue #22)

**Completed:**
- `lib/xola/env.ts` (new): `resolveXolaEnv()` selects base URL + key from `XOLA_ENV` (`sandbox` | `prod`, defaults sandbox). Lazy per-call resolution — no module-load side effects so tests + Vercel build import freely without env set.
- `lib/xola/client.ts` (new): `xolaFetch<T>(path, options)` + `XolaError`. `X-API-Key` + `Accept: application/json` headers. Retry on 5xx/429/network (3 attempts default, exp backoff 250/500/1000ms, 429 honors numeric `Retry-After` capped at 30s). Fails fast on other 4xx. Path-normalization guard rejects missing leading slash. `parseBody` wrapped on both success + error paths.
- `tests/unit/xola-client.test.ts` (new): 9 cases via Node 22 `node --test` with `globalThis.fetch` stubbed. Covers happy / 4xx no-retry / 5xx retries-then-throws / 429 + Retry-After / 429 without Retry-After / network-retry / missing key / prod URL / leading-slash guard.
- `.env.example`: added `XOLA_ENV=sandbox` line under existing Xola block.
- `package.json`: `"test:unit": "node --experimental-strip-types --test 'tests/unit/**/*.test.ts'"`.
- `tsconfig.json`: `allowImportingTsExtensions: true` so `import "./env.ts"` works for both Next compile and `node --test` — same source, two runtimes.

**Verified locally:**
- 9/9 unit tests pass (`npm run test:unit`).
- `npm run typecheck` clean.
- `npm run lint` clean.
- `npm run build` clean.
- **Live sandbox** through the new client: `GET sandbox.xola.com/api/experiences?limit=1` → 200, first experience `BLUE ICE JETBOATING ~ Twentymile Glacier Lake`.

**Code review:** @code-review (Sonnet) — 2 FIX addressed in `3783f5c`: (1) path-normalization guard (caller passing `'api/foo'` would have produced `'https://sandbox.xola.comapi/foo'`), (2) wrap `parseBody` on the 2xx path so a body-read failure surfaces as `XolaError` instead of an untyped throw + swallow `parseBody` errors on the error path so they don't mask the original status. 4 OBSERVE / 2 NIT deferred — most useful one: `MAX_RETRY_AFTER_MS=30_000` is a real product decision worth revisiting before Phase 4 bulk write-back (Vercel function timeout).
**PR:** [#30](https://github.com/mobiustripper42/crewbook/pull/30)
**Points:** 5
**Branch:** task/1.1-xola-client
**Opened at:** 2026-05-19T19:20:38Z

**Notes:**
- Test framework choice (Node `--test` not Playwright) is deliberate — pure server lib, no DOM, no route to mount. Captured in the PR description so future contributors don't add Playwright wrappers reflexively.
- `XOLA_ENV` defaults to sandbox. Phase 6.4 (prod cutover) flips one env var; no code change.
- Phase 1.2 (#23, pull experiences) is now unblocked — it imports `xolaFetch` and adds a thin per-endpoint wrapper.
- `XolaError.body` is `unknown` for now. Phase 1.2+ wrappers will need a small typed response shape per endpoint; cheaper to add it when we know what we're parsing.

## Task 2: Phase 1.2 — Pull Xola experiences + local cache (issue #23)

**Completed:**
- `supabase/migrations/20260519221028_xola_experiences.sql` (new): `xola_experiences` table (id text pk, name not null, description, raw jsonb, synced_at timestamptz). RLS enabled, no policies — service-role bypasses for the sync writer; admin-read policy deferred to Phase 5.
- `lib/supabase/server.ts` (new): `getSupabaseAdmin()` lazy singleton, service-role client. Inline TODO for `import "server-only"` when Phase 5 installs the package alongside the per-request client.
- `lib/xola/experiences.ts` (new): `fetchAllExperiences()` paginates `/api/experiences?limit=100` via `paging.next` cursor (Xola caps page size at 100; sandbox confirmed). Stuck-cursor guard bails when the same `next` URL is seen twice — sandbox-observed quirk. `MAX_PAGES=50` safety cap. `syncExperiences()` validates rows (drops + warn-logs malformed), upserts via injectable writer (`defaultWriter` = supabase upsert; tests inject a no-DB writer).
- `tests/unit/xola-experiences.test.ts` (new): 8 cases — single-page, cursor chain, stuck-cursor, MAX_PAGES cap, empty data, sync writes via writer, empty sync, skips-malformed-rows.
- `supabase/tests/rls_xola_experiences.sql` (new): pgTAP — table exists, RLS on, anon-select-empty (with a pre-inserted row to deny), anon-insert raises 42501. Note in-file on why UPDATE/DELETE under RLS-no-policy can't be tested the same way (silently affect 0 rows via USING filter).
- `lib/supabase/types.ts`: regenerated via `npm run db:types` to include the new table.
- `package.json` + `package-lock.json`: `@supabase/supabase-js` runtime dep (user installed; package.json was pre-modified before this branch was cut).

**Verified locally:**
- 17/17 unit tests (`npm run test:unit`).
- pgTAP 13/13 across both rls test files (`supabase test db`).
- `npm run typecheck` + `npm run lint` + `npm run build` clean.
- **Live sandbox** through `syncExperiences()`: 200 rows landed in `xola_experiences` (200 unique ids confirmed via `docker exec supabase_db_crewbook psql ...`). Stuck-cursor guard fired on page 3 (sandbox returned the same `paging.next` URL twice + 500 on the third call) — graceful end-of-list, no error surfaced.

**Code review:** @code-review (Sonnet) — 1 FIX + 2 OBSERVE addressed in `22257bd`: (1) validate `row.id`+`row.name` in `syncExperiences` (drop malformed + warn-log; was: crash with PK violation on a missing-id row), (2) drop speculative `synced_at` index (nothing queries by it; re-add when 1.6 needs it), (3) anon-insert-throws RLS assertion. Deferred OBSERVE: stale-row deletion policy (no soft-delete / no compute-and-delete-missing yet — fine for append-mostly experiences, becomes a real concern for orders/events in 1.4–1.6; worth a DEC entry before that lands), cached admin client survives service-role key rotation (low risk; flag for Phase 5), pagination pattern is load-bearing now and should be extracted to a `paginate<T>` helper before 1.3 copies it. 2 NIT deferred (double-cast on `raw`, MAX_PAGES test passes by off-by-one coincidence).
**PR:** [#31](https://github.com/mobiustripper42/crewbook/pull/31)
**Points:** 3 (estimated 2; carried supabase-js install + server-side admin client foundation — flag for `/retro` reconciliation)
**Branch:** task/1.2-pull-experiences
**Opened at:** 2026-05-19T22:40:54Z

**Notes:**
- Sandbox is contaminated with other accounts' test data — 200+ experiences. Prod will be ~20 (Drew's catalog). Single-page returns are the expected steady state.
- Stuck-cursor guard is sandbox-quirk-specific. If prod paginates cleanly the guard is a quiet no-op; if prod replicates the bug the guard handles it. Phase 6.4 cutover should re-verify pagination behavior end-to-end.
- `defaultWriter` uses a dynamic `await import("../supabase/server.ts")` to keep server-only code out of the test module graph (tests always inject a writer; the dynamic import never resolves in test runs).
- Stale-row policy: deliberately not added in this PR. Decide before Phase 1.4 (orders) writes its loader — soft-delete column vs. compute-missing-and-delete. Worth a DEC entry once 1.4 starts.
- First migration since 0.3. After this PR merges, `lib/supabase/types.ts` drift is the live concern again — regen + commit after every schema-touching PR.

**Next Steps:**

**Context:**
