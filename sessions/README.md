# Sessions branch (DEC-014)

Per-session files for crewbook live on this orphan branch (zero shared history with `main`). Access via `.sessions-worktree/` from the project root — see seeds CLAUDE.md for the workflow.

Filename: `YYYY-MM-DD-HHMM-<dev>-<slug>.md`.

Atomic after `/its-dead` writes `status: closed` (DEC-013).
