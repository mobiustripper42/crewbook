---
session: 4
dev: eric
slug: main
branch: main
started: 2026-05-18T17:36:12Z
ended:
points:
pr_numbers: [14, 15]
status: open
transcript: /home/eric/.claude/projects/-home-eric-crewbook/429a4f0e-a728-462e-80db-2d080a59db91.jsonl
---

# Session 4 — main

<!-- Task blocks appended by /kill-this, one per task. -->

## Task 1: Init Next.js 16 + Tailwind 4 + shadcn (base-mira/mist)

**Completed:**
- Scaffolded the runtime stack via `npx shadcn@latest init --preset b7CSfQ4Xo --base base --template next` (tweakcn preset). Result: Next 16 / React 19 / Tailwind 4 / Base UI primitives / `next-themes` / lucide / shadcn 4.
- Theme locked: `base-mira` style + `mist` base + blue primary `oklch(0.5 0.134 242)`, `--radius: 0.625rem`. Full light + dark token sets in `app/globals.css`.
- Fonts: swapped preset's Nunito Sans → **Raleway** (user pick). Geist Mono kept. Both via `next/font/google` in `app/layout.tsx`.
- Wired `NEXT_PUBLIC_APP_VERSION` forward in `next.config.mjs` (CLAUDE.md VersionTag prereq).
- Updated `docs/BRAND.md` Visual Direction block + `.claude/agents/ui-reviewer.md` Active Theme block + `CLAUDE.md` Stack line to reflect the locked choices.
- `.gitignore`: added `next-env.d.ts`, `*.tsbuildinfo`, `*.pem` (scaffold-side entries that ours was missing).

**Code review:** @code-review (Sonnet) — 4 FIX items, all addressed in commit `2b7d81c`: circular `--font-sans` self-alias in `globals.css @theme` dropped + `--font-mono` mapping added; trailing newlines on `globals.css` and `.prettierignore`; duplicate `font-sans` className removed from `layout.tsx`; `src/lib/...` and `src/components/...` paths in CLAUDE.md corrected to flat layout. OBSERVE items (Tailwind-4-config doc note, `.nvmrc` pin) deferred — not blocking.
**PR:** [#14](https://github.com/mobiustripper42/crewbook/pull/14)
**Points:** 3
**Branch:** task/0.1-init-nextjs
**Opened at:** 2026-05-18T18:14:00Z

**Notes:**
- Stack drift vs CLAUDE.md prior baseline: Tailwind 4 uses CSS-first config (`@import "tailwindcss"`, no `tailwind.config.ts`); ESLint flat config; `eslint.config.mjs`. CLAUDE.md `Commands` / `Conventions` blocks don't explicitly contradict this but assume Tailwind 3 / Next 14 syntax in places.
- Issue #8 acceptance said "Geist Sans wired up" — substituted Raleway per decision during planning. BRAND.md updated to match; closing #8 via PR.
- No DEC-113 written for the theme/font/mode picks. Worth one if we want a single record beyond BRAND.md.

## Task 2: Supabase initial schema + RLS baseline (DEC-113)

**Completed:**
- Spawned `@architect` (Session 4 mid-stream) to vet a potential pivot off Supabase + a potential PIN-based auth replacement. Both rejected with reasoning. DEC-101 + DEC-104 amended in-place with the architect's drafted rationale; DEC-104 also notes the PIN consideration + why it was deferred (60-day refresh token already gives near-zero in-season friction).
- Wrote **DEC-113** — initial RLS posture: `profiles`/`shifts`/`assignments` allow authenticated read + admin write; `scheduling_runs` is admin-only end-to-end. PROJECT_PLAN.md Key Decisions table updated.
- Created `supabase/` via `supabase init` (project_id: `crewbook`).
- Schema migration `20260518190000_initial_schema.sql`: 4 tables (profiles, shifts, assignments, scheduling_runs) + `set_updated_at()` trigger + check constraints + indexes. Notable invariants: `assignments` uses `unique(shift_id, profile_id)` + partial unique indexes on captain/mate (allows multi-shore-staff per duffy_rental shift); `shifts` has `check (end_time > start_time)` locking in the no-cross-midnight assumption; `scheduling_runs.triggered_by` references `auth.users(id)`.
- RLS migration `20260518190100_initial_rls.sql`: `is_admin()` helper (stable, NOT security definer) + 13 policies across the 4 tables.
- `supabase/seed.sql`: one local-dev admin (`admin@brewboat.local` / `brewboat-dev`) inserted into `auth.users` + `profiles`.
- `package.json`: added `supabase@^2.100.0` devDep and `db:start` / `db:stop` / `db:reset` / `db:push` (via `safe-supabase.sh`) / `db:types` (with `mkdir -p lib/supabase`) / `db:test` scripts.
- `scripts/safe-supabase.sh` chmod +x (was already in repo; never made executable).
- `.claude/prod-supabase-refs` created locally as empty placeholder (gitignored per DEC-009).

**Code review:** @code-review (Sonnet) — 1 BLOCK, 3 FIX, 3 OBSERVE, 1 NIT. All BLOCK/FIX/NIT addressed in commit `bdf7b28`. OBSERVE items (first-admin bootstrap in prod, pgcrypto extension assumption) deferred with reasoning in the PR description — not blocking.
**PR:** [#15](https://github.com/mobiustripper42/crewbook/pull/15)
**Points:** 2
**Branch:** task/0.3-supabase-schema
**Opened at:** 2026-05-18T19:35:00Z

**Notes:**
- Schema-only PR — no Supabase project linked yet. User will create the BrewBoat project on their free-tier Supabase account, run `supabase link`, then `npm install && npx supabase start && npx supabase db reset` to validate the migrations replay cleanly.
- `xola_orders`, `xola_events`, `product_type_lookup` (DEC-108), `availability` (Phase 3.1) all deferred to their natural phases — not in 0.3 acceptance.
- pgTAP RLS tests deferred to Phase 0.7 (test infra task) per DEC-109.
- Architect session deepened DEC-104 enough that the prior "TBD — picked in Phase 0.1" pattern for theme is the only Phase-0 decision still TBD in BRAND.md — and that one is now decided. DECISIONS.md should be in good shape for the rest of Phase 0.

**Next Steps:**
- Merge PR #14 (Phase 0.1 scaffold) + PR #15 (this) in any order.
- Pick next Phase 0 task: 0.4 (Vercel deploy + env vars), 0.6 (Xola sandbox + App Store reg — start early, human approval has latency), 0.7 (Playwright + axe-core + pgTAP test infra), or 0.01 (doc review sweep — lower stakes).
- After PR #15 merges + you create the BrewBoat Supabase project, run the test plan in PR #15 to validate the schema replays locally.
- Phase 0.4 will need the "first admin in prod" bootstrap pattern documented (OBSERVE item deferred from this PR).

**Context:**
- Session anchor is `main` (no platform-cut `claude/*` this time). Per CLAUDE.md, cut `task/0.1-…` branch once plan is approved.
- Pre-session cleanup: pruned 3 stale `claude/*` remote branches (`init-crewbook-seed-QKyVk`, `crewbook-seeds-template-v3-L2ZQw`, `baseline-sweep-crewbook-seeds-iPeg2`). All had been superseded or merged via PR #13.
- One stale-open session file remains on the sessions branch: `2026-05-13-…-spink-template-v3-l2zqw.md`. Historical, deferred per user.
- Repo is currently docs/skills only — no `package.json`, no Next.js scaffold yet. Task 0.1 introduces the runtime stack. After it lands, version-bump skills (DEC-007) will start firing at `/retro`.
- Task 0.1 acceptance criteria include theme selection — must update `.claude/agents/ui-reviewer.md` Active Theme block + `docs/BRAND.md` Style line as part of the task.
