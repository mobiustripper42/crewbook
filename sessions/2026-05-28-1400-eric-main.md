---
session: 10
dev: eric
slug: main
branch: main
started: 2026-05-28T14:00:15Z
ended: 2026-05-31T01:26:53Z
points: 13
pr_numbers: [51, 53, 54]
status: closed
transcript: /home/eric/.claude/projects/-home-eric-crewbook/a6ac57c9-4b9b-4b50-86c4-66682da9b52f.jsonl
---

# Session 10 — main

<!-- Task blocks appended by /kill-this, one per task. -->

## Task 1: Phase 2.0 — synthetic known-good shift fixture week (issue #50)

**Completed:**
- `tests/fixtures/shift-agent/week-2026-06-01.json` (new) — synthetic input week in real Xola shape: 4 boats, 9 events / 9 orders. Scenarios: Boat A 3-slot run, Boat B one-slot gap, Boat C two-slot gap, Boat D multi-order slot (6+4=10 guests) + a standalone zero-order event.
- `tests/fixtures/shift-agent/week-2026-06-01.expected.json` (new) — hand-written answer key: 5 shifts. Each `{date, boat_resource_id, product_type, start, end, roles, covered_event_ids}`. Boat C splits into two; Boat D's empty event yields no shift.
- `tests/fixtures/shift-agent/README.md` (new) — Drew's six operator rules, scenario table, boat-ID legend, and a note that in-run gaps are implicit (no event under `reserved=true`), not zero-order events.
- `tests/unit/shift-fixture.test.ts` (new) — sanity guard: feeds the fixture through `lib/xola/mapping.ts` (brewboat slots, no warnings, 10-guest sum) + asserts the answer key matches the rules incl. rule-3 within-shift and cross-shift gap checks.

**Decisions captured:** Drew's event→shift rules — same-boat run collapse; slot = 2h (shift end = last event start + 2h); one-slot gap bridged, two-slot gap splits; booked-events-only; brewboat = captain+mate; cross-boat combination out of scope. Fixture is synthetic by design (real-2025 validation is 2.5/#49, which needs prod Xola read access — not yet wired).

**Verified locally:** `npm run test:unit` 129/129 (+11 new); `typecheck` + `lint` + `build` clean. No migration/RLS/UI — pgTAP + Playwright untouched.

**Code review:** @code-review (Sonnet) — clean to merge. Three cleanups all addressed pre-PR: added rule-3 gap assertions (marquee scenario wasn't asserted; a merged Boat C split would have passed before), README implicit-gaps clarification, fixed contradictory `reserved: 1` → `0` on the empty event.

**PR:** [#51](https://github.com/mobiustripper42/crewbook/pull/51)
**Points:** 3 (held — landed on estimate)
**Branch:** task/2.0-shift-fixture-week
**Opened at:** 2026-05-28T20:45:44Z

## Task 2: Phase 2.1 — shift-generation agent: prompt + structured output (issue #45)

**Completed:**
- `lib/agents/client.ts` (new) — Anthropic factory, `AGENT_MODEL = claude-sonnet-4-6` per DEC-103 (lazy key resolve; constructs fresh per call after review found a cache-pinning footgun).
- `lib/agents/shift-agent-prompt.ts` (new) — system prompt encoding the six operator rules verbatim + a SMALL DISTINCT few-shot (1-boat 1-gap example, not the 2.0 fixture) so grading tests generalization rather than recall. `SHIFT_OUTPUT_SCHEMA` (JSON Schema for `output_config.format`), deterministic `buildUserPrompt` (sorted by boat+start), `parseShiftAgentOutput` parser/validator.
- `lib/agents/shift-agent.ts` (new) — `generateShifts({slots, weekStart, responder?})`. Injectable responder so unit tests run the full build→parse pipeline without the SDK or a key. Default responder uses `messages.create` with `output_config.format` (json_schema) + `cache_control` ephemeral on the system prefix; typed errors on non-end_turn stop reasons (includes output_tokens + content-block snippet for 2.2's retry policy).
- `lib/agents/grade-shifts.ts` (new) — pure compare keyed by (date, boat, start). Returns `{matched, mismatched, missing, extra, score}` — reused by 2.5.
- `tests/unit/shift-agent.test.ts` (new) — 13 cases: buildUserPrompt sort + determinism, parseShiftAgentOutput valid/non-JSON/missing-shifts/bad-role/missing-field, generateShifts injected-responder happy path + propagated parse failure, gradeShifts perfect/missing/extra/wrong-end.
- `scripts/eval-shift-agent.ts` (new) — live, key-gated eval. NOT in CI. One real Sonnet call per run; loads `.env.local`, maps the fixture, runs the agent, grades, exits non-zero on any mismatch.
- `docs/TEST.md` — agent-eval section + ANTHROPIC_API_KEY setup line + note that unit tests mock the SDK.
- `package.json` / `package-lock.json` — adds `@anthropic-ai/sdk` (0.100.1).

**Live eval:** first call against the 2.0 fixture (`week-2026-06-01`) — **100% / 5 of 5 shifts exact**. All six rules applied correctly (3-slot run, one-slot bridge, two-slot split, multi-order summing, booked-only). No prompt tuning needed.

**Decisions made (worth flagging for 2.2):**
- **Structured outputs over forced tool-use.** `output_config.format` with raw JSON Schema (no zod dep). Simpler, schema-guarantees parseable JSON, SDK-recommended for Sonnet 4.6.
- **Few-shot is deliberately distinct** from the 2.0 fixture week so grading tests generalization, not recall. Eval week stays the only graded ground truth.
- **DEC-103 = Sonnet, intentional.** The claude-api skill defaults to Opus; project decision overrides. Inline comment in client.ts so future-me doesn't "fix" it.

**Verified locally:**
- 142/142 unit tests pass (+13 new); `typecheck` + `lint` + `build` clean.
- Live eval: 100%.
- No migration, no RLS, no UI — pgTAP + Playwright untouched.

**Code review:** @code-review (Sonnet) — pipeline shape correct, SDK usage verified against `@anthropic-ai/sdk@0.100.1`, schema in supported subset, caching design sound. Two bugs + four cleanups addressed pre-PR (typed error on non-end_turn; dropped client cache; hoisted defaultResponder; named max_tokens ceiling; clarified cache-comment; better roles error). **Deferred to 2.2/2.5** (called out as out-of-scope for 2.1): token-usage logging into scheduling_runs, retry/backoff, prompt versioning, model-ID date pinning, multi-boat / DST / empty-week / truncation fixtures.

**PR:** [#53](https://github.com/mobiustripper42/crewbook/pull/53)
**Points:** 5 (held — landed on the estimate. Agent worked first try; 2.0 fixture paid off as the deterministic grading target.)
**Branch:** task/2.1-shift-agent
**Opened at:** 2026-05-30T02:44:54Z

## Task 3: Phase 2.2 — reservation → shift pipeline (issue #46)

**Completed:**
- `supabase/migrations/20260530030456_shifts_and_scheduling_runs.sql` (new) — ALTER on the Phase 0.3 base tables. Adds week_start, boat_resource_id, roles (jsonb), covered_event_ids (text[]), scheduling_run_id (FK) to `shifts`; week_start, model, input/output/cache_*_tokens to `scheduling_runs`. Partial unique index `(week_start, boat_resource_id, start_time)` anchors the regenerate gate; pre-Phase-2 admin-entered rows (NULL week_start) coexist.
- `supabase/tests/rls_shifts_phase2.sql` + `rls_scheduling_runs_phase2.sql` (new) — 19 pgTAP cases pinning the new columns + the partial unique index + admin-only scheduling_runs RLS (exercised with a real non-admin profile + JWT sub claim).
- `lib/agents/shift-agent-prompt.ts` — added `SHIFT_AGENT_PROMPT_VERSION = "2.1.0"` (the 2.1 review's explicit deferral).
- `lib/agents/shift-agent.ts` — `ShiftResponder` now returns `{raw, usage?}`; `generateShifts` returns `{shifts, raw, usage?, model, promptVersion}` so the pipeline can record cost into scheduling_runs.
- `lib/shifts/persist.ts` (new) — pure builders: `buildShiftRow` (parses local ISO with required `±HH:MM` offset — rejects `Z` and naive ISO to avoid silent wall-clock corruption), `buildRunRow` (success + failure paths), `shouldRegenerate` (gate).
- `app/admin/reservations/actions.ts` — `generateShiftsForWeek` server action. Flow: SHIFT_AGENT_DISABLED env kill-switch → gate (error unless force) → load slots → call agent → insert run row → (if force) delete existing → insert shifts → revalidate. Failure-path audit insert is wrapped so its own failure doesn't mask the model error.
- `components/admin/generate-shifts-form.tsx` (new) — Client Component with force checkbox, useActionState for pending/result/error.
- `app/admin/reservations/page.tsx` — mounts the form + three pre-existing a11y fixes (thead and product-type badge color contrast at 12px/10px; table scroll container needed tabIndex+role on mobile to satisfy axe).
- `tests/unit/shifts-persist.test.ts` (new, 11 cases) + updated `tests/unit/shift-agent.test.ts` for new responder shape.
- `tests/admin-reservations.spec.ts` — Generate Shifts case uses an empty-mirror week to exercise wiring without API spend.
- `docs/TEST.md` — Generate Shifts flow + force gate + data-state line for shifts/scheduling_runs.

**Decisions / surprises:**
- **Phase 0.3 already created shifts + scheduling_runs + assignments with placeholder shapes.** My initial migration tried to CREATE TABLE and failed. Rewrote as ALTER with `if not exists`. Kept the existing `shift_date / start_time / end_time` split (Phase 0 deliberately used local time + a `end > start` check — no midnight crossing) instead of bringing in `timestamptz` columns. Code-review flagged: "if-not-exists everywhere" is fine for Phase 0/1 catch-up but not for Phase 2+; convention worth documenting in DECISIONS.md.
- **Existing `agent_version` column doubles as prompt_version** — no need for a separate column.
- **DEC-107 vs DEC-113 RLS posture confirmed:** shifts = authenticated read / admin write; scheduling_runs = admin-only on every op (raw agent IO not crew-facing).

**Verified locally:**
- `supabase test db` 62/62 across 9 files (+19 new).
- `npm run test:unit` 154/154 (+12 new).
- `typecheck` + `lint` + `build` clean.
- `npx playwright test tests/admin-reservations.spec.ts` 12/12 across mobile/tablet/desktop (+3 new × 3 viewports for Generate Shifts).
- NO live agent call from this session — the action is wired but not exercised; that's manual UI verification on a non-CI run.

**Code review:** @code-review (Sonnet) — 5 bugs + 4 cleanups, all addressed pre-PR. Three bugs were real: loose ISO parser (silent wall-clock corruption on `Z`/naive), swallowed audit-insert failure in the outer catch, and dynamic imports in the catch block. Added SHIFT_AGENT_DISABLED env kill-switch for the pre-5.1 cost-exposure window the reviewer flagged. **Deferred:** regenerate-race lock (in_progress run row — design needs thought), boat_label population (2.3 render scope), centralizing text-muted-foreground contrast token (ui-reviewer scope), if-not-exists convention doc.

**Estimate honesty:** issue said 3 pts; landed as **5**. Migration + agent surface change + persist + action + UI + a11y cleanup is real 5 work. Flag at retro.

**PR:** [#54](https://github.com/mobiustripper42/crewbook/pull/54)
**Points:** 5 (was estimated 3)
**Branch:** task/2.2-shift-pipeline
**Opened at:** 2026-05-30T03:36:39Z

**Next Steps:**
- **Phase 2.3** — Shift review UI (#47, 5 pts). The natural follow-on. Render generated shifts grouped by day; needs the `boat_label` join the 2.2 review flagged as deferred (resolve from xola_resources or an admin-editable lookup like product_type_lookup). Worth a fresh session for UX focus.
- **Quick housekeeping (anytime, no issue):** fix the stale `docs/TEST.md` claim that "xola_events has 5 events for the week of 2026-05-18" — actual mirror has 1 event in 5/18 week + 4 events in 6/6 week (Saturday of 6/1 week). Surfaced when Eric clicked Generate Shifts on the default week and got "Generated 1 shift" (correctly, but the docs implied 5).
- **Pre-5.1 cost guard:** consider setting `SHIFT_AGENT_DISABLED=1` in Vercel until Phase 5.1 auth lands. Action is reachable on `/admin/*` and makes billed Sonnet calls.
- **2.2 deferred follow-ups** (track separately, don't merge into 2.3): regenerate-race lock via an `in_progress` scheduling_runs row; centralize `text-muted-foreground` contrast token (three patches this PR — token problem); `if-not-exists` migration-convention DEC.

**Context:**
- **Phase 2 is 13 / 24 pts in** (2.0 + 2.1 + 2.2 shipped, all merged). 11 pts left: 2.3 (5), 2.4 (5), 2.5 (3).
- **The agent works.** First live eval against the 2.0 fixture scored 100% / 5 of 5 shifts exact; rules 1–5 all encoded correctly. No prompt tuning needed in 2.1; the 2.0 fixture was the right shape of grading target.
- **Phase 0.3 already created shifts + assignments + scheduling_runs** with placeholder shapes. 2.2 ALTERed them rather than CREATEing — surprised me mid-session. Existing `agent_version` doubles as the prompt-version column; existing `shift_date / start_time / end_time` split (date + time, no midnight crossing) was kept rather than re-introducing `timestamptz`.
- **DEC-103 = Sonnet for both agents.** The `claude-api` skill defaults to Opus and is adamant; project decision overrides. `lib/agents/client.ts` has a load-bearing comment so future-me doesn't "fix" it.
- **DEC-116 stays load-bearing** for Phase 2+: `profiles.id ≠ auth.uid()`. New code joining Supabase Auth must use `auth_user_id`. 2.2's pgTAP scheduling-runs non-admin test now exercises a real non-admin (seeds profile + auth.users + JWT sub).
- **TEST.md mirror-state line is stale** (see Next Steps); the data-state section needs a one-line correction.
- **Two Supabase stacks on this dev box** (crewbook + bushel) coexist; `db reset` on one would conflict — check `docker ps` before any destructive op.
- **PR test plans now consistently click-by-click + verb-first** (the lesson from session 9's PR #44). Both 2.1 and 2.2 PR bodies follow the pattern; readable on the phone in the GitHub mobile app.
