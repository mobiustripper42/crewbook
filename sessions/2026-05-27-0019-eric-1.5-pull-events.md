---
session: 9
dev: eric
slug: 1.5-pull-events
branch: task/1.5-pull-events
started: 2026-05-27T00:19:25Z
ended:
points:
pr_numbers: [41, 42, 43, 44]
status: open
transcript: /home/eric/.claude/projects/-home-eric-crewbook/5f197562-b7d7-442d-a95a-345574416821.jsonl
---

# Session 9 — 1.5-pull-events

<!-- Task blocks appended by /kill-this, one per task. -->

## Task 1: Phase 1.6 — Map Xola orders → events → boats → time slots (issue #27)

**Completed:**
- `supabase/migrations/20260527005509_product_type_lookup.sql` (new) — DEC-108 table: `xola_name` PK → `product_type` (check-constrained to `brewboat` / `captained_duffy` / `duffy_rental`). DEC-113 RLS (authenticated read, admin write). Seeded with the one currently-active product (`Brewboat Tour - captained` → `brewboat`).
- `supabase/tests/rls_product_type_lookup.sql` (new) — 7 pgTAP cases: table exists, RLS on, seeded row visible, check-constraint rejects invalid type, anon SELECT empty, anon INSERT 42501, non-admin authenticated INSERT 42501.
- `lib/xola/mapping.ts` (new) — pure mapping layer with three entry points: `groupOrderItemsByEvent(orders) → Map<eventId, items[]>` walks `order.items[].event.id` (multi-item-order-safe); `mapEventToSlot(event, items, lookup) → TimeSlot` produces one slot; `mapSlotsForWeek(events, orders, lookup) → TimeSlot[]` is the e2e entrypoint. `TimeSlot` includes `event_id`, `seller_id`, `experience_id`, `product_name`, `product_type` (nullable), `start`/`end` (ISO passthrough from Xola), `boat_resource_id`, `total_guests`, `order_count`, `status_breakdown`, `warnings`.
- **Soft handling for unknown product names** (user-approved option a): unknown name → `product_type: null` + warning, slot still surfaces. Schedule never breaks on a Xola rename. Same soft pattern for missing title, multiple `resourceUsages`, missing start/end.
- `tests/unit/xola-mapping.test.ts` (new) — 14 cases: happy path, multi-order-per-event guest summing (party of 6 + party of 4 → 10), open slot (no items), unknown name warning, missing-title warning, multi-resource warning, no-resource (no warning), cancelled status (700) counted in breakdown, multi-item order across two events, real sandbox payload shape end-to-end.
- `lib/supabase/types.ts` — regenerated to include `product_type_lookup`.
- `docs/DECISIONS.md`, `docs/PROJECT_PLAN.md`, `docs/SPEC.md` — corrected the "19 distinct product names" historical reference; live set is small (one product) with defunct names lingering in Xola. Sample-data line in PROJECT_PLAN.md corrected (no `tests/fixtures/xola-sample-data.json` exists in the repo; the local Supabase mirror from Session 8 is the test data).

**Verified locally:**
- 83/83 unit tests pass (`npm run test:unit`) — was 69; +14.
- 32/32 pgTAP tests across 6 files (`supabase test db`) — was 25; +7.
- `npm run typecheck` + `npm run lint` + `npm run build` clean.
- Mapper exercised against the real-sandbox payload shape from the Phase 1.5 mirror (5 events / 5 orders / 1 brewboat experience) inside the test file — no live Xola call needed; mapping is pure.

**Code review:** @code-review (Sonnet) — no FIXes, nothing blocks merge. Notable OBSERVEs:
- **`total_guests` sums confirmed + cancelled items into one scalar** by design (DEC-115 says cancellations are real history). Pinned by test. Phase 2 shift agent needs to read `status_breakdown` rather than `total_guests` alone if it wants confirmed-only capacity — flag for the prompt design.
- **`product_type_lookup.updated_at` has no `before update` trigger** — defaults on insert, stale on update. Informational column. Either add a trigger or drop the column once 1.7 confirms it's unused.
- **`Record<string, unknown>` typing throughout the mapper.** Defensible given the passthrough `XolaEvent` / `XolaOrder` design, but ~10 inline shape-narrowings will accumulate; worth extracting a narrow `XolaOrderItem` interface in 1.7 / Phase 2 once item access patterns stabilize.
- NIT: pgTAP authenticated-role block proves non-admin INSERT rejected but doesn't independently confirm non-admin SELECT works (covered indirectly via the anon-denied counterexample). One line to close the loop; not pulled now.

**PR:** [#41](https://github.com/mobiustripper42/crewbook/pull/41)
**Points:** 4 (estimated 5; landed under because the live set turned out to be one product — no need for a 19-row seed or multi-name fixture work. DEC-108 docs correction + soft-handling design + multi-order test scenarios absorbed the remaining time.)
**Branch:** task/1.6-map-orders-events
**Opened at:** 2026-05-27T00:55:09Z

**Notes:**
- **DEC-108 "19 distinct product names" was historical noise.** User confirmed: many products were added in Xola over the years but are defunct; only `Brewboat Tour - captained` produces events today. DEC entry, SPEC.md, and PROJECT_PLAN.md all corrected. The lookup table design still stands — the configurability rationale is "Xola lets sellers rename products at will, and we don't want a code change for every rename."
- **Sample-data fixture file (`tests/fixtures/xola-sample-data.json`) doesn't exist** despite CLAUDE.md and PROJECT_PLAN.md referencing it. The actual test data is the live local Supabase mirror from Session 8. PROJECT_PLAN.md updated to match reality; CLAUDE.md still has the stale reference (intentionally not touched here — would be a separate docs-consistency task).
- **Mapping is pure / stateless** — no `mapped_slots` table, no persistence. Re-run any time. If Phase 1.7's admin UI wants a server-rendered view, we can add a Postgres view or a Server Component cache then; deferring until the query pattern is known.
- **Cancelled-guest accounting** is the one design judgment call. `total_guests` sums everything; `status_breakdown` preserves the per-status counts. Downstream code chooses which to use. The Phase 2 shift agent prompt design needs to be aware (flagged in PR body).
- **Two Supabase stacks up concurrently** on this dev box (`crewbook` + `bushel`) — both currently running, didn't conflict because crewbook was already up when this session started. Per memory `feedback_supabase_single_stack`, if a future task needs to `db reset` bushel I'd need to stop crewbook first.

## Task 2: Phase 1.7 — Admin "Pull Week" page + reservation table (issue #28)

**Completed:**
- `app/admin/reservations/page.tsx` (new) — Server Component. Reads `?week=YYYY-MM-DD` (defaults to `2026-05-18`, the seeded sandbox week, with an italic "showing seeded sandbox week" note when no `?week=` was passed). Snaps to Monday of the requested week via `startOfWeek`. Queries `xola_events` + `xola_orders` + `product_type_lookup` in parallel via `getSupabaseAdmin`, builds the lookup map, runs `mapSlotsForWeek`, filters to the week range, renders header + week picker + table or empty state. Error banner if any of the Supabase queries fail.
- `app/admin/reservations/actions.ts` (new) — `pullWeek(formData)` Server Action. Sequential `syncEvents` then `syncOrders` for the visible week's range (sequential keeps the Xola rate-limit hit honest and surfaces a first-failure error before doubling); calls `revalidatePath` on success. Returns `{ok, events, orders, error}`.
- `app/admin/reservations/week.ts` (new) — pure helpers, no framework imports: `startOfWeek` (snap to Monday, Mon–Sun week per user direction; DST-spring-forward-safe via noon-UTC construction), `weekRange` (Mon → Sun+6 inclusive), `summarizeStatuses` (collapses `{200: 2, 700: 1}` to `"2 confirmed, 1 cancelled"`; surfaces unknown codes as `"N status XXX"` so they don't silently disappear), `shortResourceId` (last-8-chars with ellipsis), `sortSlotsForDisplay` (start → event_id, immutable), `filterSlotsInWeek` (inclusive range; preserves slots with missing start so warnings surface).
- `components/admin/pull-week-form.tsx` (new) — Client Component. React 19 `useActionState` to surface the action result inline (success: "Synced N events / M orders"; failure: error text in destructive color). Submit button disables while pending. testid-tagged for Playwright.
- `tests/unit/admin-week.test.ts` (new) — 14 cases. Notable coverage: DST spring-forward week (2026-03-08, week starts 2026-03-02), Mon–Sun semantics (Sunday snaps to previous Monday), month/year boundary crossings, status aggregation across 200+202 codes, ordered output (confirmed before cancelled), unknown-code surfacing, sort stability + immutability.
- `tests/admin-reservations.spec.ts` (new) — 3 Playwright cases × 3 viewports (mobile 375 / tablet 768 / desktop 1440): default-view render with Pull Week + table present and "Brewboat Tour - captained" rows visible; week picker navigates to `?week=YYYY-MM-DD` and shows the empty state; axe-core a11y (no Serious/Critical). Locator-fix mid-test: `getByText("Week of 2024-01-01")` matched both header `<p>` and empty-state `<div>` case-insensitively — tightened to a `data-testid="week-range"`.

**Display choices:**
- **Columns:** Date · Time · Product · Boat · Guests · Orders · Status.
- **Unknown product types:** product name in italics + small `⚠` glyph with the mapper warnings concatenated in the `title` attribute. Mobile users don't get hover (NIT flagged in review for a future disclosure widget).
- **Boat column:** `…74080020` truncation with full id on hover. No boat-name lookup table exists; deferred (out of 3-pt scope).
- **`product_type` badge:** small monospace chip after the product name when known (`brewboat`).
- **Default route shows seeded data:** without `?week=`, lands on `2026-05-18` so first-load shows the 5 mirrored events. Note rendered to make the choice explicit.

**Auth posture:**
- Page is **unauthenticated** until Phase 5.1 ships magic-link + `profiles.is_admin` gate. Uses `getSupabaseAdmin` (service-role) — documented in the page header comment. Not linked from any public surface. Tracking-issue tag `phase-5.1` recommended in PR body so the route ends up behind the gate when auth lands.

**Stacked-PR situation:** 1.7 depends on the not-yet-merged 1.6 work (`lib/xola/mapping.ts`, `product_type_lookup` table, regenerated types). Initial typecheck failed because 1.7 was branched off main. Options surfaced to user; user chose "merge #41 first, then rebase 1.7 onto fresh main." Done via `git merge --ff-only origin/main` (the harness denied `git reset --hard` so used the non-destructive equivalent — same end-state since 1.7 had no commits ahead of old-main yet).

**Verified locally:**
- 107/107 unit tests pass (`npm run test:unit`) — was 93 on this branch; +14.
- 9/9 Playwright across 3 viewports (`npx playwright test tests/admin-reservations.spec.ts`) — first run had 3 failures (locator strict-mode), fixed via `data-testid="week-range"` and re-run green.
- `npm run typecheck` + `npm run lint` + `npm run build` clean. Build correctly tags `/admin/reservations` as ƒ (dynamic).
- Mobile gate (375px) covered by the mobile Playwright project — 3/3 passing.
- pgTAP unchanged (no migration touched).

**Code review:** @code-review (Sonnet) — no FIXes, nothing blocks merge. OBSERVEs:
- **Unfiltered Supabase reads.** `select("raw")` on `xola_events` + `xola_orders` pulls everything every render. Fine at 5 rows in the seeded sandbox; needs a Postgres-side filter (gte/lte on a generated `start_at` column or expression index on `raw->>'start'`) before the mirror exceeds a few hundred rows. Flag for revisit when prod data flows.
- **`row.raw as unknown as XolaEvent` cast.** Generated Supabase types treat jsonb as `Json`; the passthrough Xola types are the right runtime shape. Deliberate; surfaced in PR body.
- **Auth gap.** `/admin/reservations` is reachable without auth. Documented in code; needs an issue tagged `phase-5.1` so the route gets covered by the magic-link gate.
- NITs (not pulled): mobile-friendly disclosure for the ⚠ warnings instead of `title` hover; deduping `STATUS_LABELS` against `DEFAULT_STATUS_CODES` once a second consumer appears; minor pluralization branching in the form.

**PR:** [#42](https://github.com/mobiustripper42/crewbook/pull/42)
**Points:** 3 (held — landed on the estimate. Stacked-PR rebase added a small detour but the 1.6 work merging cleanly made it trivial.)
**Branch:** task/1.7-pull-week-button
**Opened at:** 2026-05-27T01:55:09Z

**Notes:**
- **`@/lib/xola/mapping` and `lib/supabase/types.ts` were only on the 1.6 branch** when 1.7 started — branching 1.7 off main caused the first typecheck to fail. Resolved by merging #41 to main first, then fast-forwarding 1.7 onto main. For future tasks that build directly on the previous task's primitives: branch off the previous task's branch (stack the PRs) OR wait for the predecessor to merge before cutting. The "stacked PR + target the predecessor's branch as base" approach is what the seeds workflow expects; user preferred the sequential merge here. Worth flagging as a workflow note.
- **Pull Week button was not exercised live** — the seeded sandbox is the existing state, so hitting Pull Week would re-sync the same 5 events / 5 orders (idempotent upsert). The action's success-result text is exercised in the Client Component path; first true end-to-end refresh happens against the next live sandbox/prod data change. Flagged in PR test plan.
- **Boat name resolution** is the next missing piece for Drew's experience. Phase 1.7 ships with `…74080020`-style short IDs; Drew needs to see "Boat A" / "Boat B". Options for a future task: (a) `boat_resource_lookup` table parallel to `product_type_lookup`, admin-editable; (b) pull from a Xola resources endpoint if one exists. Worth its own discussion + DEC before implementing.
- **Mon–Sun week semantics** chosen per user direction. The crew "weekend" is Sunday afternoon brunch; the week boundary after Sunday matches operator intuition. ISO-8601 also defines Monday as week start, so this is convention-aligned.
- **Default route lands on seeded week** rather than current week — temporary V1 choice so first-load shows data. Once prod data flows (Phase 6.4), flip the default to "current week" and remove the "showing seeded sandbox week" note.

## Task 3: docs/TEST.md — canonical "how to test BrewBoat right now"

**Completed:**
- `docs/TEST.md` (new) — captures the local-only V1 test path (npm run dev + mill-dev:3000, local Supabase via supabase start, Xola sandbox via .env.local). Documents the seeded mirror state (5 events / 5 orders / 1 guide / 200 experiences / 1 lookup row) so PRs know what data to expect. Six "when this changes" triggers listed (remote Supabase, prod Xola cutover, Phase 5.1 auth, staging, fixtures, CI) with a one-liner each on what to add when they ship.
- `CLAUDE.md` — Key Docs row added for TEST.md with explicit "keep current" maintenance contract.

**Trigger:** user feedback mid-session — "each project does testing slightly differently; I never know exactly what to do to test." Need to stop re-deriving this each session. TEST.md is the canonical answer.

**Not a planned phase task.** No issue, no points, no estimate. Ad-hoc doc cleanup shipped while the test path was fresh in mind.

**Verified locally:**
- No code changes — typecheck / build / tests untouched, didn't re-run.
- Markdown syntax verified.

**Code review:** skipped (docs-only).
**PR:** [#43](https://github.com/mobiustripper42/crewbook/pull/43)
**Points:** 1 (estimated 0 — unplanned; small ad-hoc.)
**Branch:** task/docs-test-md
**Opened at:** 2026-05-27T02:25:00Z

**Notes:**
- **vercel.json fix on sessions branch** also shipped this session, outside /kill-this. User noticed Vercel was still building the orphan sessions branch despite a `git.deploymentEnabled.sessions: false` on main. Root cause: Vercel reads the config from the branch being pushed, not from main; orphan branch had no vercel.json. Fix: added vercel.json with the same config to the sessions branch via the worktree (commit `1863ea1` on sessions). One-line workflow note worth remembering — same logic applies if another orphan branch (gh-pages, etc.) ever lands.
- **TEST.md "keep current" contract is the load-bearing part.** Without the explicit "update in the same PR as the change" line and the matching CLAUDE.md row, this doc rots in 3 phases. The CLAUDE.md pointer is what makes it a contract instead of a snapshot.

## Task 4: Phase 1.8 — Staff cross-reference (DEC-116 + admin UI) (issue #29)

**Completed:**

DEC-116 architecture:
- `docs/DECISIONS.md` — DEC-116 "Profile identity decoupled from auth.users." Standalone `profiles.id` (gen_random_uuid default), nullable `auth_user_id uuid references auth.users(id) on delete set null`, nullable `xola_guide_id text references xola_guides(id) on delete set null`. Why: Phases 2-4 need pre-auth crew profile rows; old `profiles.id references auth.users(id) on delete cascade` forced operational toil or hidden admin-API complexity to create profiles before crew first logged in. Phase 5.1 magic-link fills `auth_user_id` on first sign-in.
- `supabase/migrations/20260527021619_profile_identity.sql` — adds the two new columns, backfills `auth_user_id = id` for existing rows (safe because the old FK guaranteed every id mapped to a real auth.users row), drops `profiles_id_fkey`, changes `profiles.id` default to `gen_random_uuid()`, REWRITES `public.is_admin()` to match `auth_user_id = auth.uid()` instead of `id = auth.uid()`, adds partial index `profiles_auth_user_id_idx where auth_user_id is not null`.
- `supabase/seed.sql` — admin row now sets both `id` (kept stable at `00000000-0000-0000-0000-000000000001` for fixtures) AND `auth_user_id` (same uuid).
- `supabase/tests/rls_profile_identity.sql` — 11 pgTAP cases: column shapes, uniqueness, seeded admin auth_user_id backfilled correctly, **is_admin() returns true for seeded admin under their auth.uid()** (the load-bearing assertion), is_admin() returns false for unknown uid, profile inserts allowed with null auth_user_id (the new ability), xola_guide_id on_delete set null behavior.

Admin UI:
- `app/admin/staff/page.tsx` — Server Component (`force-dynamic`). Parallel-queries `xola_guides` + `profiles`. Per-guide card shows linked profile (with role badges + Unlink + Edit Roles) OR auto-match-pre-selected picker + "Create new profile from this guide". Separate "Orphan profiles" section for profiles with no xola_guide_id (Drew, pre-Phase-5.1 placeholders). Error banner for failed Supabase queries.
- `app/admin/staff/actions.ts` — 4 server actions returning `{ok, error?, profile_id?}`. `linkGuideToProfile` clears any prior link to that guide first (honors xola_guide_id uniqueness). `createProfileFromGuide` synthesizes a placeholder email (`xola-<id>@brewboat.local`) when the guide has none (NOT NULL + UNIQUE constraint on profiles.email).
- `app/admin/staff/match.ts` — pure scorer. Email-equal (case-insensitive) = 100. Exact-name (whitespace + case normalized) = 80. ≥2 token overlap = 40-70 band. Single-token first-name overlap when both sides multi-token is **deliberately not a match** — too noisy.
- `components/admin/profile-edit-form.tsx` — Client Component using React 19 `useActionState`. Role checkboxes for is_admin/is_captain/is_mate/is_shore + `boat_quals` as comma-separated text input. Save button with pending state.
- `tests/unit/admin-match.test.ts` — 11 cases. Notable: email-null path (sandbox returns this), case-insensitive email, normalization in exact-name path, single-first-name noise rejection.
- `tests/admin-staff.spec.ts` — 3 cases × 3 viewports. beforeEach seeds a known xola_guide row via the Supabase client + admin role key; afterEach cleans it up. Tests don't depend on sandbox sync state.
- `tests/global-setup.ts` — loads `.env.local` into the Playwright test process (Next loads it for the dev server, but tests run in a separate Node process). Parser strips trailing `# comments` after values (the existing .env.local has inline annotations).
- `playwright.config.ts` — globalSetup wired.
- `lib/supabase/types.ts` — regenerated.

**Verified locally:**
- `supabase db reset` replays all 8 migrations cleanly.
- 118/118 unit tests pass (was 107; +11).
- 43/43 pgTAP across 7 files (was 32; +11).
- `npm run typecheck` + `lint` + `build` clean. `/admin/staff` correctly tagged ƒ (dynamic).
- 9/9 Playwright (`tests/admin-staff.spec.ts`) across mobile / tablet / desktop. axe-core: no Serious/Critical violations.

**In-flight fixes during build:**
- pgTAP missed `xola_guides.raw` NOT NULL when inserting a test guide → added `'{}'::jsonb`.
- match.ts had a dead-code "1-token name match" branch (exact-equal fires first) → removed + test updated to assert the normalization path instead.
- /admin/staff was statically pre-rendered (no dynamic API call to opt in) → `export const dynamic = "force-dynamic"`.
- .env parser didn't strip inline comments → values like `http://127.0.0.1:54321                  # ...` failed URL validation. Fixed by stripping ` #` and after.
- `<select>` lacked an associated label → axe critical. Added `aria-label`.
- `"Local Admin"` Playwright locator matched both the picker `<option>` and the orphan row text → scoped to the orphans section.
- Role badge `text-muted-foreground` at 10px failed axe contrast → bumped to `text-foreground text-xs`.

**Code review:** @code-review (Sonnet) — no FIXes, nothing blocks merge. OBSERVEs:
- **Post-DEC-116, `profiles.id` is no longer guaranteed to correspond to an `auth.users` row.** Anything assuming `profiles.id == auth.uid()` is wrong and must use `auth_user_id`. Only `is_admin()` made that assumption pre-DEC-116; the migration rewrites it. Re-check grep before Phase 5.1.
- **Link actions are non-transactional.** `linkGuideToProfile` is "clear prior link, set new link" — two concurrent admin clicks on the same profile could race. Fine for V1 single-admin; revisit if a second admin appears.
- **`linkAction`/`unlinkAction`/`createAction` adapters in page.tsx swallow ActionResult errors** to satisfy the Server `<form action={...}>` `void`-return contract. Errors surface only via revalidated row state. Follow-up: lift these into Client Components with useActionState like `ProfileEditForm`.
- NIT addressed: `export const dynamic` moved after import block (separate fixup commit `5b5f064`).
- NIT not pulled: `createProfileFromGuide` doesn't capture the clear-prior-link error (the insert below surfaces real problems).

**PR:** [#44](https://github.com/mobiustripper42/crewbook/pull/44)
**Points:** 5 (held — landed on the estimate. Migration + UI + 11 pgTAP + 11 unit + 3 Playwright fit. The "two separate PRs A/B" split discussed up-front was waived per user direction.)
**Branch:** task/1.8-staff-xref
**Opened at:** 2026-05-27T02:31:23Z

**Notes:**
- **DEC-116 was the unblock.** The original task estimate (5 pts) assumed adding a column + UI. The schema mismatch (`profiles.id` FK to `auth.users.id` blocks pre-auth creation) was the hidden complexity. Surfaced via the three architectural-choice questions before code; user picked option (a) "restructure" which is the cleaner long-term design. The migration is small but load-bearing — `is_admin()` rewrite is the single most important thing in this PR.
- **`db reset` wipes the live-synced mirror.** Before this task: 5 events / 5 orders / 1 guide / 200 experiences. After: empty. Re-run Pull Week or the sandbox sync scripts to repopulate. The Playwright spec is now self-sufficient (seeds its own row) so testing doesn't depend on this.
- **Three pages now exist** under `/admin`: `/admin/reservations` (1.7), `/admin/staff` (1.8). Both unauthenticated until Phase 5.1. The "auth gap" track-issue list is growing — worth a single Phase 5.1 prep task that lists every public admin route + the gate it needs.
- **Match scorer is intentionally coarse.** No Levenshtein, no soundex. Drew always confirms in the picker — cost of a wrong auto-suggest is one extra click. Don't over-engineer.
- **createProfileFromGuide synthesizes `xola-<id>@brewboat.local` for null emails.** Drew can edit later via the role-edit form (well, no — email isn't editable there yet). Worth its own follow-up: "edit profile email/name" in the role-edit form, or a separate Edit Profile page.

**Next Steps:**

**Context:**
